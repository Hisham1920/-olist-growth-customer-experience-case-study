# Data setup

Download the [Brazilian E-Commerce Public Dataset by Olist](https://www.kaggle.com/datasets/olistbr/brazilian-ecommerce) and place the nine CSV files in `data/raw/`.

The original CSV files and the large processed order fact table are excluded from version control. Compact aggregate outputs may be regenerated by running `analysis/build_analysis.py`.

